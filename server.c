#include "src/net.h"
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    net_server_run();
    //
    // char buf[] = {'a', 'A', '\n'};
    // int i = 0;

    // for (i = 0; i < 3; i++) {
    //     int val = (int) *buf + i;
    //     printf("val %d : %d\n", i, val);
    // }


    // return 0;
}
